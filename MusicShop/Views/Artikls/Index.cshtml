@model IEnumerable<MusicShop.Models.Artikl>

@{ ViewData["Title"] = "Index"; }

<h1>Popis artikala</h1>

<p>
    <a asp-action="Create">Dodaj novi artikl</a>

</p>


    @using (Html.BeginForm())
    {
    <p>
    Title: @Html.TextBox("searchStr") <br />
    <input type="submit" value="Traži" />
    </p>
    }
    

    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Ime)
                </th>

                <th>
                    @Html.DisplayNameFor(model => model.Cijena)
                </th>

                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Ime)
    </td>

    <td>
        @Html.DisplayFor(modelItem => item.Cijena)
    </td>

    <td>
        <a asp-action="Edit" asp-route-id="@item.SifArtikla">Edit</a> |
        <a asp-action="Details" asp-route-id="@item.SifArtikla">Details</a> |
        <a asp-action="Delete" asp-route-id="@item.SifArtikla">Delete</a>
    </td>
</tr>
}
        </tbody>
    </table>
